// Set the text alignment to center
draw_set_halign(fa_center);
draw_set_valign(fa_middle);

// Calculate the center position
var center_x = 960 / 2;
var center_y = 540 / 2;

// Draw the text at the center
draw_text(center_x, center_y, "Waiting for players");
